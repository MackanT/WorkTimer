version: "3.8"
services:
  worktimer:
    build: .
    image: worktimer-app:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data_dpg.db:/app/data_dpg.db
    # environment:
    # Add any environment variables here if needed
    # EXAMPLE_VAR: value
    restart: unless-stopped
