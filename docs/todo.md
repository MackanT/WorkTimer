## BUGS
- [ ] markdown code not working in chromium (?)
- [ ] #c92490

- [x] Markdown images can not be on same line as a marker, get put on the row below
- [x] (IDEA) Highlihgt text with "==Some Text==" -> yellow/other color highlight?
- [x] Notepad drawers do not store open/closed, so going back and forth between pages auto opens everythin
- [x] list of available items in board (create new object), current column/status/etc are wrong on startup. Need to switch work-item type back and forth once for it to show correct values. Initially it shows every possible combination
- [ ] tracker upload file with item(?)
- [x] tracker create branches (?) - auto create option with a user-story (?)
- [ ] option to view "mulitle" boards at once - see for all customers (?)
- [x] Option to "define" the standard comment/message for tracker. Currently these are hardcoded - but being able to define them in the app would be nice. Use like {{contact}} or something to indicate that the contact drop-down value should be preset

